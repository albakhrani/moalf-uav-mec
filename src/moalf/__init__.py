"""moalf — reference simulation for the MOALF-UAV-MEC paper (IEEE IoT Journal, 2025).

This top-level package intentionally does NOT import the submodules below, so the
package remains importable while the logic modules are still stubs. Import the
concrete modules directly once they are implemented.

Source of truth for behavior: notes/corrected_spec.md.
"""

__version__ = "0.0.0"
