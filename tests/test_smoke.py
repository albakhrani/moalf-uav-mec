"""Placeholder smoke test.

Exists so the test suite is runnable from day one. Intentionally trivial: it
does NOT import the stub logic modules (those raise NotImplementedError on
import by design). Replace/extend as real modules land.
"""


def test_smoke():
    """The test harness itself works."""
    assert True
