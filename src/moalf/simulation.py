"""simulation — main loop (Algorithm 1, MOALF-UAV-MEC).

Will implement the top-level per-time-slot control loop that observes state,
queries MORL -> MPC -> APSO -> Lyapunov, applies adjustments, and steps the
environment (Algorithm 1). Reads all parameters from config/default.yaml; no
hard-coded values. Source of truth: notes/corrected_spec.md.
Equation references: notes/method_extraction.md §3.D.

Stub: logic intentionally not implemented yet (awaiting finalized spec).
"""

raise NotImplementedError(
    "simulation loop not implemented yet — awaiting finalized notes/corrected_spec.md"
)
