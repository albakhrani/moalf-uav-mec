# Runtime + dev dependencies for the MOALF-UAV-MEC reference simulation.
# Pinned to the versions used for development and the reported reproduction
# (Python 3.9.10). Exact bit-reproducibility of the MORL/DQN results also
# requires matching the torch build (dev/CI used the +cu117 build; training runs
# on CPU and is seeded).

numpy==1.26.3
pyyaml==6.0.3
torch==2.0.1

# dev / testing
pytest==8.4.2
