"""system_model — deterministic physical models (channel, computation, energy).

Build order: these deterministic parts come first, before the
optimizers. Modules are stubs until the finalized spec lands.
"""
